const _ = require('lodash');

const numbers = [1, 2, 3, 4, 5];
const reversedNumber = _.reverse(numbers);
console.log('---',reversedNumber);

const str = "abdul kadir khan";
const capitalizeWord = _.capitalize(str);
console.log('num...',capitalizeWord);
